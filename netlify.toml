[functions]
  directory = "netlify/functions"

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200

# Ensure client-side React Router routes (including /auth-callback) resolve correctly
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
